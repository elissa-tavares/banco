/**
 * This interface defines a contract for services that provide withdrawal functionality,
 * extending the core capabilities defined in the `Withdrawal` interface.
 */
package org.example.service.withdrawal;

import org.example.core.Withdrawal;

public interface WithdrawalServiceClient extends Withdrawal {
}
